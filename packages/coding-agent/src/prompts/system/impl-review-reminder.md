<system-reminder>
An implementation review is pending for the approved debate plan.
Choose one action now:

- Continue implementing the remaining plan steps.
- Submit the finished implementation: write the plan slug as plain text to `xd://propose`.
- Fix the reviewer's blocking findings, then resubmit.
- Use `ask` when a decision needs the user.
</system-reminder>
