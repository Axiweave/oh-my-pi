The canonical debate plan at `{{planFilePath}}` changed after reviewer consensus.
Human approval was not applied.
Read the current plan, verify the changed bytes, and submit it through `xd://propose` for a new review.
